import{_ as e,c as a,a2 as d,o}from"./chunks/framework.CJ3Fe3Yg.js";const i="/assets/image%20(84).Q_SUivmc.png",c="/assets/image%20(85).BXXzyHMP.png",s="/assets/image%20(89).BdtC9Hbi.png",g=JSON.parse('{"title":"特性說明","description":"","frontmatter":{},"headers":[],"relativePath":"moran-docs/07.其他模式/00.魔然字词方案/特性说明.md","filePath":"moran-docs/07.其他模式/00.魔然字词方案/特性说明.md"}'),r={name:"moran-docs/07.其他模式/00.魔然字词方案/特性说明.md"};function l(n,t,h,p,m,b){return o(),a("div",null,t[0]||(t[0]=[d('<h1 id="特性說明" tabindex="-1">特性說明 <a class="header-anchor" href="#特性說明" aria-label="Permalink to &quot;特性說明&quot;">​</a></h1><h2 id="反查" tabindex="-1">反查 <a class="header-anchor" href="#反查" aria-label="Permalink to &quot;反查&quot;">​</a></h2><table tabindex="0"><thead><tr><th>反查方式</th><th>編碼</th><th>例子</th></tr></thead><tbody><tr><td><code>SY``</code></td><td>依雙拼反查形輔</td><td><code>yj``</code> 可查得「言演」等字</td></tr><tr><td><code>``FF</code></td><td>依形輔反查讀音</td><td><code>``cm</code> 可查得「葉茶」等字</td></tr></tbody></table><p>反查只能反查到碼表中的所有打法，而無法查到全碼。如欲查詢全碼和拆分，可切換到「魔然」模式。</p><h2 id="次選鍵" tabindex="-1">次選鍵 <a class="header-anchor" href="#次選鍵" aria-label="Permalink to &quot;次選鍵&quot;">​</a></h2><p>分號 <code>;</code> 爲次選鍵，等價於按數字鍵 <code>2</code>。</p><p>默認模式的分號不等價於 <code>2</code>。</p><h2 id="手動造詞鍵" tabindex="-1">手動造詞鍵 <a class="header-anchor" href="#手動造詞鍵" aria-label="Permalink to &quot;手動造詞鍵&quot;">​</a></h2><p>引號 <code>&#39;</code> 爲字詞模式的手動造詞鍵。</p><ul><li>當輸入 <code>&#39;</code> 後，輸入框提示 〔造詞〕，此時可輸入詞語編碼，字與字之間使用 <code>&#39;</code> （引號）隔開。️</li><li>如果單字有重碼，可以像一般的拼音輸入法一樣選字。</li><li>輸入上屏後，上屏的詞語會被記錄到字詞模式的用戶詞庫中，之後可依據通常的多字詞規則輸入該詞語。</li><li>如果用戶造詞與系統詞的編碼重複，則用戶詞後置。</li></ul><p>以「君不見黃河之水天上來」爲例，造詞後，可使用正常的多字詞編碼 <code>jbjl</code> 輸入該詞語。</p><figure><img src="'+i+`" alt=""><figcaption></figcaption></figure><h2 id="自動造詞-根據上屏歷史" tabindex="-1">自動造詞（根據上屏歷史） <a class="header-anchor" href="#自動造詞-根據上屏歷史" aria-label="Permalink to &quot;自動造詞（根據上屏歷史）&quot;">​</a></h2><p>Rime 支持根據上屏歷史自動造詞，換句話說，如果你連續輸入了「曾」「國」「藩」三字，則會自動造出一則臨時詞「曾國藩」。該功能默認關閉。</p><p>臨時詞具有 ☯️ 標記，選擇一次後，臨時詞轉爲永久詞，失去該標記。</p><p>同碼的臨時詞數量有限，老臨時詞有可能被新的臨時詞覆蓋掉。</p><p>要開啓此功能，編輯 <code>moran_fixed.custom.yaml</code> 並輸入如下內容：</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">patch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  fixed/encode_commit_history</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span></code></pre></div><h2 id="連續上屏" tabindex="-1">連續上屏 <a class="header-anchor" href="#連續上屏" aria-label="Permalink to &quot;連續上屏&quot;">​</a></h2><p>使用 <code>=</code> 鍵可呼出上次上屏內容。如輸入 <code>nihk</code> 上屏「你好」後，按 <code>=</code> 可得到如下候選列表：</p><figure><img src="`+c+'" alt=""><figcaption></figcaption></figure><p>在 <code>moran_fixed.schema.yaml</code> 中提供了長按 <code>=</code> 連續上屏的默認設置，搜索 <code>custom_bindings</code> 後可找到相關內容，解除註釋就可以雙擊 <code>=</code> 重複上屏上次的輸入。</p><figure><img src="'+s+'" alt=""><figcaption></figcaption></figure><p>注意：這裏的 <code>4</code> 與 <code>symbols.yaml</code> 中的設置高度相關，僅在默認配置中有意義。如果修改了 <code>symbols.yaml</code> 的 <code>=</code> 相關設置，則這裏的 <code>4</code> 也大概率需要做對應的變更。</p><h2 id="o-引導的符號" tabindex="-1">o 引導的符號 <a class="header-anchor" href="#o-引導的符號" aria-label="Permalink to &quot;o 引導的符號&quot;">​</a></h2><table tabindex="0"><thead><tr><th>種類</th><th>說明</th><th>示例</th></tr></thead><tbody><tr><td>os</td><td>數字</td><td>①②</td></tr><tr><td>ob</td><td>製表符</td><td>┌├</td></tr><tr><td>（and more……TBD）</td><td></td><td></td></tr></tbody></table>',26)]))}const f=e(r,[["render",l]]);export{g as __pageData,f as default};
